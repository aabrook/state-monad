{
  "name": "state-monad",
  "version": "1.0.0",
  "description": "An implementation of the state monad, following fantasy-land conventions",
  "main": "index.js",
  "author": "aabrook at gmail",
  "license": "MIT",
  "scripts": {
    "test": "RELEASE_ENVIRONMENT=test NODE_ENV=test mocha $(find . -not -regex '.*node_modules.*' | grep __tests__/.*-test.js)",
    "lint": "standard --verbose | snazzy"
  },
  "devDependencies": {
    "mocha": "^3.5.0",
    "snazzy": "^7.0.0",
    "standard": "^10.0.3"
  },
  "standard": {
    "globals": [
      "_",
      "describe",
      "before",
      "beforeEach",
      "after",
      "afterEach",
      "it"
    ]
  }
}
